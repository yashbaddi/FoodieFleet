This is the context that i would be leaving this project.

In past few days i have setup nginx pnpm turborepo and followed by dockerfile.

Then i merged them in docker-compose

i moved from building docker to pushing them in dockerhub container regirstry

and the ec2 would pull from docker container regirstry all this you could see in github actions.

There were two things that were pending to get ci/cd up and running is 
1. Add frontend env args inside the github actions secret variables. 
2. update it inside the env of the ec2. 

Once done you would be moving to phase-6